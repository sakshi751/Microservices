name: Deploy to Cloud Foundry

on:
  push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Cloud Foundry CLI
        uses: elliottpope/cloudfoundry-cli-action@v6
        with:
          CF_API: https://api.cf.ca10.hana.ondemand.com
          USERNAME: richa.khosla@canadapost.postescanada.ca
          PASSWORD: Request@2023
          ORG: CPEA-Innovapost Inc_development-dn6yj4ti
          SPACE: devspace
          COMMAND: cf help -a
